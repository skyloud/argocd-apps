generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "Blackbox"

configMapGenerator:
  - name: dashboard-blackbox
    files: [blackbox.json]
