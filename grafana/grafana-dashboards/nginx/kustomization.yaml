generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "Nginx"

configMapGenerator:
  - name: dashboard-nginx
    files: [nginx.json]
