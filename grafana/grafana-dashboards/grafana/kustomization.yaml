generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "Grafana"

configMapGenerator:
  - name: grafana-loki
    files: [grafana-loki.json]
