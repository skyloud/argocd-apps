generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "Longhorn"

configMapGenerator:
  - name: dashboard-longhorn
    files: [longhorn.json]
