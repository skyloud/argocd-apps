generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "CoreDNS"

configMapGenerator:
  - name: dashboard-coredns
    files: [coredns.json]