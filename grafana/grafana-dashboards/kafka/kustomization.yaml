generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "Kafka"

configMapGenerator:
  - name: dashboard-kafka
    files: [kafka.json]
