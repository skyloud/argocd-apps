generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "ArgoCD"

configMapGenerator:
  - name: dashboard-argocd
    files: [argocd.json]
