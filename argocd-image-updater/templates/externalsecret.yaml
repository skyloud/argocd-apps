{{- if .Values.app.externalSecret.enabled }}
{{- if not .Values.app.externalSecret.secretStoreRef.name }}
{{- fail "app.externalSecret.secretStoreRef.name is required when app.externalSecret.enabled=true" }}
{{- end }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.app.externalSecret.target.name | default (include "argocd-image-updater.fullname" .) }}
  labels:
    {{- include "argocd-image-updater.labels" . | nindent 4 }}
spec:
  {{- with .Values.app.externalSecret.refreshInterval }}
  refreshInterval: {{ . }}
  {{- end }}
  secretStoreRef:
    name: {{ .Values.app.externalSecret.secretStoreRef.name }}
    kind: {{ .Values.app.externalSecret.secretStoreRef.kind | default "ClusterSecretStore" }}
  target:
    name: {{ .Values.app.externalSecret.target.name | default (include "argocd-image-updater.fullname" .) }}
    creationPolicy: {{ .Values.app.externalSecret.target.creationPolicy | default "Owner" }}
    deletionPolicy: {{ .Values.app.externalSecret.target.deletionPolicy | default "Retain" }}
  {{- with .Values.app.externalSecret.data }}
  data:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.app.externalSecret.dataFrom }}
  dataFrom:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
